{
  "deploy": {
    "test": {
      "user": "donut",
      "host": "5.196.206.62",
      "ref": "origin/master",
      "repo": "git@github.com:dbrugne/donut.git",
      "path": "/home/donut/app",
      "post-deploy": "npm update && grunt pm2 --deployEnvironment test && grunt build && pm2 stop all && pm2 delete all && pm2 start ecosystem.json --env test",
      "env": {
        "NODE_ENV": "test"
      }
    },
    "production": {
      "user": "donut",
      "host": "5.196.206.61",
      "ref": "origin/production",
      "repo": "git@github.com:dbrugne/donut.git",
      "path": "/home/donut/app",
      "post-deploy": "npm update && grunt pm2 --deployEnvironment production && grunt build --uglify true && pm2 stop all && pm2 delete all && pm2 start ecosystem.json --env production",
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}