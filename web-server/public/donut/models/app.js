'use strict';
define([
  'underscore',
  'backbone'
], function (_, Backbone) {
  var AppModel = Backbone.Model.extend({
    initialize: function (options) {
      //
    }

  });

  return new AppModel();
});
